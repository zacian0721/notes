import math
def f1(n,a,x):
    int i
    float p=a[0]
    for i in range(n):
        p +=(a[i]*pow(x,i))  #最表面的表达方式
n=int(input())
x=float(input())
b[]
for i in range(n):
    b.append(float(input()))   #真要运行得逐个输入系数
result=f1(n,b[],x)
print(result)
